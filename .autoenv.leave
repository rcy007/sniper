if [[ "$PWD" != "$PROJECT_ROOT"* ]]; then
    # Deactivate only if outside the project root and its subdirectories
    echo 'Exiting sniper-dev-env conda enivronment!'
    conda deactivate
    unset PROJECT_ROOT
fi
